# Bug 分析守则

## 第一条：证据优先

**任何诊断动作之前，先回答：我的假设如何解释当前证据？**

| 证据类型 | 必须回答的问题 |
|---------|-------------|
| SIGSEGV | 哪块内存被非法访问了？→ bt 看调用栈 |
| SIGABRT | 哪个 assert 失败了？→ bt + 寄存器看条件 |
| 空白/错色/闪烁 | 哪个渲染阶段出了问题？→ 抓帧/验证脚本 |
| 链接错误 | 哪个符号、哪个 .o 文件、哪行代码触发？→ 错误信息里都有 |
| 无响应/卡死 | 哪个线程在等什么？→ bt 看所有线程 |

**证据 → 假设 → 验证，三步缺一不可。没有证据的假设 = 随机猜测。**

## 第二条：三样工具，任何 bug 先用

1. **`gdb -batch -ex run -ex bt`** — 崩溃？看调用栈。卡死？看线程。永远先跑这个
2. **`git log --all --oneline --grep=<关键词>`** — 做过吗？谁做的？哪个提交？
3. **`diff`** — 能工作和不能工作的版本，逐行对比

用结果说话，不用"可能是"。

## 第三条：二分法定位

能工作的版本 → 能工作的版本 + 一个变化 → 测 → 不工作？定位到那一行。

不能同时改三个东西再测——改成一次改一个，每个变化后都测。

## 第四条：不假设环境

- "环境变了"、"驱动升级了"、"Qt 版本不同"——这些都是假设。验证方法是切回已知工作的提交，编译运行。能跑？环境没变。
- 只要说"环境可能变了"，等于承认自己不知道根因。

## 第五条：看已有代码

项目里已经有类似的实现？先读那个。已经有相关的 git 提交？先看 diff。已经有写过的文档？先打开。

不看已有的东西就去写新的 = 浪费时间。

## 第六条：写之前想

每次改代码之前，回答三个问题：
1. 这一行改完，我**确信**会修好吗？（确信 ≠ "可能"、"试试看"）
2. 我的确信基于什么证据？（bt？diff？日志？）
3. 如果没修好，下一步做什么？

答不上来？停下来收集证据，不要改代码。

## 第七条：承认不知道

说"我不知道"比编造一个答案更快、更准确。不知道就去找——GDB、git log、diff、文档——找不到再问。

## 检查清单

每次开始排查 bug 时过一遍：

- [ ] 拿到崩溃 dump / 日志 / 截图了？
- [ ] `bt` 看了吗？调用栈指向哪一行？
- [ ] 已知工作的版本是哪个？
- [ ] 和当前版本 diff 了吗？
- [ ] git log 里有没有做过类似的事？
- [ ] 如果改代码，我确信会修好吗？确信的证据是什么？
